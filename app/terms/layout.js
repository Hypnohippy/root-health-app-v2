export const metadata = {
  title: "Terms of use",
  description: "The terms that apply when using Root Health.",
  alternates: { canonical: "/terms" },
  robots: { index: true, follow: true },
};

export default function TermsLayout({ children }) {
  return children;
}
