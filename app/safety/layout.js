export const metadata = {
  title: "Safety and boundaries",
  description:
    "The safety principles and boundaries that guide Root Health wellbeing support.",
  alternates: { canonical: "/safety" },
  robots: { index: true, follow: true },
};

export default function SafetyLayout({ children }) {
  return children;
}
